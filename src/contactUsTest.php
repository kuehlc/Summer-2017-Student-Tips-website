<?php
/** Imports the db_connectionTest php file */
require 'db_connectionTest.php';

/**
 * Retrieves the connection from the connect method in db_connectionTest
 */
$conn = connect ();

$name = $_POST['full-name'];
$email = $_POST['email'];
$comment = $_POST['comment'];

/** Get timestamp */
$time_stamp = date("Y-m-d") . " " . date("H:i:s");

/** SQL insert statement */
$sql = "INSERT INTO student_tips.comment VALUES ('$name', '$email', '$comment','$time_stamp')";

try {
	$conn->exec ( $sql );
} catch (PDOException $e) {
	echo $sql . "<br>" . $e->getMessage ();
}
?>

<!-- FIGURE OUT WHERE THIS GOES -->
<?php
$nameErr = $emailErr = $genderErr = $websiteErr = "";
$name = $email = $gender = $comment = $website = "";

if ($_SERVER["REQUEST_METHOD"] == "POST") {
  if (empty($_POST["name"])) {
    $nameErr = "Name is required";
  } else {
    $name = test_input($_POST["name"]);
    // check if name only contains letters and whitespace
    if (!preg_match("/^[a-zA-Z ]*$/",$name)) {
      $nameErr = "Only letters and white space allowed";
    }
  }
  
  if (empty($_POST["email"])) {
    $emailErr = "Email is required";
  } else {
    $email = test_input($_POST["email"]);
    // check if e-mail address is well-formed
    if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
      $emailErr = "Invalid email format";
    }
  }
    
  if (empty($_POST["website"])) {
    $website = "";
  } else {
    $website = test_input($_POST["website"]);
    // check if URL address syntax is valid (this regular expression also allows dashes in the URL)
    if (!preg_match("/\b(?:(?:https?|ftp):\/\/|www\.)[-a-z0-9+&@#\/%?=~_|!:,.;]*[-a-z0-9+&@#\/%=~_|]/i",$website)) {
      $websiteErr = "Invalid URL";
    }
  }

  if (empty($_POST["comment"])) {
    $comment = "";
  } else {
    $comment = test_input($_POST["comment"]);
  }

function test_input($data) {
  $data = trim($data);
  $data = stripslashes($data);
  $data = htmlspecialchars($data);
  return $data;
}

?>